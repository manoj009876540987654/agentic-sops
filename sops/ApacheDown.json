{
  "error_key": "apache_stopped",
  "risk": "low",
  "description": "Apache service is stopped on Linux VM",
  "plan": [
    {"cmd": "sudo systemctl start apache2"},
    {"cmd": "sudo systemctl is-active apache2"}
  ]
}
